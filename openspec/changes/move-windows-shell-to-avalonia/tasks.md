The windows and the tray are replaced in place (groups 3 and 4), before the application lifetime moves to Avalonia (group 5). In between, the build and the tests are the check: the app doesn't run again until group 5 is done.

## 1. Packages and build

- [ ] 1.1 Add Avalonia 12.1.1 to `Directory.Packages.props` (design D6): `Avalonia`, `Avalonia.Win32`, `Avalonia.Skia`, `Avalonia.HarfBuzz`, `Avalonia.Themes.Fluent`, and `Avalonia.Headless` for the tests. Reference the first five in `Pisum.Transcribe.csproj` and `Avalonia.Headless` in `Pisum.Transcribe.Tests.csproj`. `UseWPF` stays until task 5.5. Verify: `dotnet build Pisum.Transcribe.slnx` passes with warnings as errors, and the build output has no `Avalonia.X11.dll`, `Avalonia.Native.dll` or `Avalonia.FreeDesktop*.dll`.
- [ ] 1.2 Turn off Avalonia's build telemetry (`Avalonia.BuildServices`, D6) with the opt-out that package documents, set in `Directory.Build.props` with a comment that says why. Verify: a detailed build log (`dotnet build Pisum.Transcribe.slnx -v:d`) shows that the telemetry task doesn't run, locally and in the CI job's log.
- [ ] 1.3 Remove `global using System.Windows` from `GlobalUsing.cs` (D6). Add explicit `using` directives to the WPF files that still need them until they are replaced. Verify: `dotnet build Pisum.Transcribe.slnx` passes.

## 2. Headless tests

- [ ] 2.1 Add the headless test platform in the T1 fallback form (D11): a test app builder with `UseHeadless` and the Fluent theme, named by `[assembly: AvaloniaTestApplication]`, and a shared helper at the test project root that holds `HeadlessUnitTestSession.GetOrStartForAssembly` and runs a test body through `Session.Dispatch(…, TestContext.Current.CancellationToken)`. Verify: a smoke test, a plain `[Fact]` that opens and closes an Avalonia window through the helper, passes in `dotnet test Pisum.Transcribe.slnx`.

## 3. The windows

- [ ] 3.1 Add `ConfirmDialog`, a small modal Avalonia window with a message, **Yes** and **No**, and **No** as the default button (D8). It returns `Task<bool>`. Closing it any other way, Esc included, counts as **No**. Verify in `ConfirmDialogTests`, headless: **Yes** returns true, **No** and Esc return false, and **No** is the default button.
- [ ] 3.2 Replace `RecordingOverlayWindow` with an Avalonia window that implements `IRecordingOverlay` (D8): `ShowActivated=false`, `Topmost`, `ShowInTaskbar=false`, `WindowDecorations.None` and a transparent background. **Right after every `Show`**, it sets `WS_EX_NOACTIVATE | WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW | WS_EX_LAYERED` through `TryGetPlatformHandle` and CsWin32. It keeps `CalculateBounds` and the placement on the target window's monitor, and it ports the dot, the elapsed time and the spinner with their timings (the spinner as a keyframe animation switched on by a class). Verify:
  - `RecordingOverlayWindowTests` run headless and assert the same states as today (starting, recording with the elapsed time, transcribing, message, hidden), and the `CalculateBounds` tests pass unchanged.
  - A new `Hardware` test (`[Fact(Explicit = true)]`, `DesktopCollection`) shows, hides and shows the overlay again on the real desktop, and each time finds the four extended styles set and the foreground window unchanged.
- [ ] 3.3 Replace `ModelSetupWindow` with an Avalonia window (D8), with the port notes' replacements and `x:DataType` on every view. The license link becomes a `HyperlinkButton` that opens the URL with `Process.Start` and `UseShellExecute`, as today. The close confirmation during a download uses `ConfirmDialog`: `Closing` is cancelled first, and the window closes again after **Yes**. Opening or focusing the window restores it if it's minimized, then activates it. Verify: `dotnet build Pisum.Transcribe.slnx` passes with compiled bindings, and headless tests show that closing during a download asks, **No** keeps the window and the download, and **Yes** cancels the download and closes the window.
- [ ] 3.4 Replace `SettingsDialog` with an Avalonia window (D8): the `ListBox` navigation with each section's `IsVisible` bound to `#Navigation.SelectedIndex`, and the port notes' replacements for triggers, `GroupBox`, `Run`s, the license link and the system brushes. Model deletion and the close confirmation during a download use `ConfirmDialog`, the latter as in task 3.3. The view models don't change. Verify: `SettingsDialogTests` run headless without their STA thread and assert the same things as today, and a new test shows that deleting a model asks and that **No** keeps it.
- [ ] 3.5 When the app exits, the settings window and the model setup window close without asking, as `settings-window` requires (D8). A flag set by the shutdown skips the confirmation, and a `ConfirmDialog` that is already open closes with its owner. Verify: headless tests close each window with the flag set, once during a download and once while the confirmation shows, and both close with no confirmation left open.
- [ ] 3.6 Use the Fluent theme's default accent and density (design Open Questions), and replace `SystemColors.GrayTextBrushKey` and `ActiveBorderBrushKey` with the theme's secondary text and border brushes. Verify: screenshots of the three windows in light and dark mode, taken by hand after group 5, are attached to the pull request.

## 4. The tray

- [ ] 4.1 Change `TrayIcons` and `TrayIconService.LoadIcons` to load the six status ICOs of `add-monochrome-tray-icons` as Avalonia `WindowIcon`s instead of `System.Drawing.Icon`s (D4). Avalonia picks the frame at `SM_CXSMICON` itself. Verify: the icon tests in `TrayIconServiceTests` pass headless, and `IconFor` still picks each status icon for the light and the dark taskbar.
- [ ] 4.2 Build `TrayIconService` over Avalonia's `TrayIcon` and a `NativeMenu` (D4). **Exit** stays last. The icon is created at startup, hidden until `Show`, and keeps following the taskbar's mode. `Remove` disposes it and makes later calls no-ops. `DoubleClicked` becomes `Clicked`, raised by `TrayIcon.Clicked`. Verify: `TrayIconServiceTests` run headless and cover the order of the items with **Exit** last, `SetStatus`, the taskbar mode change, and calls after `Remove`.
- [ ] 4.3 Run the menu's visibility and header callbacks once each time the menu opens, from a class handler on `Window.WindowOpenedEvent` that matches the type name `TrayPopupRoot` (D4). Verify:
  - A unit test calls the update and checks that each item's `IsVisible` and header follow its callbacks.
  - A unit test finds a type named `TrayPopupRoot` in the `Avalonia.Win32` assembly, so an Avalonia update that renames it fails the tests.
- [ ] 4.4 Open the settings window on `Clicked` in `SettingsWindowService` (spec `settings-window`, "Opening the settings window"). A second `Clicked` while the window is open activates it and opens no second window. Verify: tests with a fake `ITrayIconService` raise `Clicked` once with the window closed (it opens) and twice in a row (one window, activated).

## 5. The application lifetime

- [ ] 5.1 Add `AvaloniaUiDispatcher : IUiDispatcher` over `Dispatcher.UIThread.InvokeAsync`, not `Post` (D7), and port `WpfUiDispatcherTests` to it, headless: the action runs on the UI thread, after the current operation, and an exception faults the task without reaching `Dispatcher.UIThread.UnhandledException`. Verify: the three ported tests pass.
- [ ] 5.2 Move `DispatcherWait` to Avalonia's `DispatcherFrame` and `Dispatcher.UIThread.PushFrame`, with its reasoning kept: a synchronous continuation ends the frame (D7). Verify: `DispatcherWaitTests` run headless without their STA thread, including the frame ended from another thread.
- [ ] 5.3 Replace `App.xaml` with an Avalonia `App` and change `Program.Main` to `AppBuilder.Configure<App>().UseWin32().UseSkia().UseHarfBuzz().StartWithClassicDesktopLifetime(args, ShutdownMode.OnExplicitShutdown)` (D6, D7). The single-instance guard, the bootstrap logger and `[STAThread]` stay. `OnFrameworkInitializationCompleted` builds the host, creates `ShutdownCoordinator`, shows the tray, loads the settings and starts the host, as `OnStartup` does today. `AppHost` registers `AvaloniaUiDispatcher`, and it stays the only code that reads the current application. Verify: `dotnet run --project src/Pisum.Transcribe` shows the tray icon, the log shows the model ready, and a dictation into Notepad works end to end.
- [ ] 5.4 Move `ShutdownCoordinator` to the Avalonia lifetime (D7): the `shutdownApplication` delegate becomes the lifetime's `Shutdown(exitCode)`, it subscribes to `Dispatcher.UIThread.UnhandledException`, and `ExitProcess` calls `TerminateProcess` through CsWin32 (add it to `NativeMethods.txt`, remove the `DllImport` in `App.xaml.cs`). The lifetime's `ShutdownRequested` handler calls `DispatcherWait.Until(RequestShutdownAsync(ShutdownReason.SessionEnd))` and never cancels. Verify:
  - `ShutdownCoordinatorTests` pass.
  - **Exit** from the tray ends the app with code 0, and the log shows `Shutting down, reason "Exit"`.
  - `spike/send-endsession.ps1` from the spike branch, pointed at `Pisum.Transcribe`, sends `ENDSESSION_CLOSEAPP` and then `ENDSESSION_LOGOFF` in two runs. Each time the log shows `Shutting down, reason "SessionEnd"`, then `Hosting stopped`, and the process ends within 5 s with code 0.
- [ ] 5.5 Remove WPF (D6): `UseWPF`, `WpfUiDispatcher` and its tests, the remaining WPF XAML and code-behind, `H.NotifyIcon.Wpf` from the project and from `Directory.Packages.props`, and `TrayBalloonNotifier` if it's still there. Update the `WpfUiDispatcherTests` mention in `InlineUiDispatcher`'s doc. Verify: `dotnet build Pisum.Transcribe.slnx` passes, `dotnet test Pisum.Transcribe.slnx` passes, and no source file refers to `System.Windows`, `System.Drawing` or `H.NotifyIcon`.

## 6. Packaging

- [ ] 6.1 Update the MSI's payload (D12). `build-msi.ps1` no longer ships `ThirdPartyNotices-Wpf.txt` and `ThirdPartyNotices-WinForms.txt`, and `packaging/third-party/dotnet-wpf-THIRD-PARTY-NOTICES.txt` and `dotnet-winforms-THIRD-PARTY-NOTICES.txt` go. Verify: `./packaging/windows/build-msi.ps1 -Version 0.1.0-dev.1` builds and validates the MSI. Record the MSI's size before and after in the pull request.
- [ ] 6.2 Extend `assert-native-dependencies.ps1` with the new native DLLs of the publish output (`libSkiaSharp.dll`, `libHarfBuzzSharp.dll`, `av_libglesv2.dll`, and any other the publish output lists) and their imports, and remove the WPF ones. Verify: the guard passes on the published folder, and it fails when one of the new DLLs is removed from it.
- [ ] 6.3 Update `THIRD-PARTY-NOTICES.md` from the publish output, not from memory (D12). Add Avalonia and MicroCom.Runtime (MIT), SkiaSharp (MIT) with Skia and the libraries `libSkiaSharp.dll` bundles from SkiaSharp's own notices, HarfBuzzSharp (MIT) and HarfBuzz (Old MIT), and ANGLE (BSD-3-Clause) for `av_libglesv2.dll`. Remove the `H.NotifyIcon` section, and remove the Windows Desktop runtime, dotnet/wpf and dotnet/winforms from the .NET section. Verify: every DLL in the published folder is covered by an entry, as the `packaging` notices requirement asks.

## 7. Documentation

- [ ] 7.1 Update `CLAUDE.md` (D13): the layout (`App.axaml`, the Avalonia windows), *UI thread* (`AvaloniaUiDispatcher`, `DispatcherWait` over Avalonia's frame), *View models*, *Tests* (the headless helper instead of STA threads), and every other WPF and H.NotifyIcon mention. Verify: `CLAUDE.md` has no mention of WPF or H.NotifyIcon left, apart from history.
- [ ] 7.2 Update `README.md` (a left click opens the settings, the Avalonia shell) and `docs/roadmap.md` with the Windows steps and the macOS steps: the four preparing changes, this change, `add-macos-shell`, and the later macOS changes its design lists. Verify: both files name the left click and no double-click, and the roadmap lists this change as the last Windows step before `add-macos-shell`.

## 8. Regression pass

- [ ] 8.1 Run `dotnet build Pisum.Transcribe.slnx`, `dotnet test Pisum.Transcribe.slnx`, the `Hardware` tests (`--filter-trait "Category=Hardware" --explicit on`) and `openspec validate move-windows-shell-to-avalonia --strict`. Verify: no warnings, all tests pass, and validation reports no issues.
- [ ] 8.2 Check by hand against the specs on Windows 11, installed from the MSI (Migration Plan step 6). Verify each:
  - dictation end to end, the overlay keeps the focus in the target window and lets clicks through, and insertion into an elevated window is refused as today
  - the overlay's placement at 100 % and at 150 %, on the target window's monitor
  - the clipboard is restored after a paste
  - a real sign-out, and a restart, with the app running: the log shows `Shutting down, reason "SessionEnd"`, and Windows doesn't list the app as blocking
  - every tray status and icon on a light and a dark taskbar, every notification, and the menu's items after each change of state (model missing, download running, update available)
  - a left click opens the settings, a right click opens the menu, and a double-click leaves one settings window in front
  - an upgrade from the previous MSI while the app runs, and an uninstall
- [ ] 8.3 Repeat task 8.2 on Windows 10 22H2. If no Windows 10 machine is available, record that it was skipped and why, as in `show-windows-notifications`.
