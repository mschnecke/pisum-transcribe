## 1. Controller

- [x] 1.1 Add `DictationMessages` (all user-facing strings from the spec) and `IDictationFeedback`. The test project already references `Microsoft.Extensions.TimeProvider.Testing`. Verify: builds.
- [x] 1.2 Implement the `DictationController` event loop (design D1: channel of events, state `Idle | Recording | Processing`, tracked processing task, `ProcessingCompleted`) with hotkey and recorder event subscriptions, and register it as a hosted service. Pass the stopping token to `StartAsync` and `InsertAsync`. Log the time from the press to the completed start at Information level. Verify: unit tests with fakes and `FakeTimeProvider` cover press → `ShowStarting`, then `StartAsync` called with `MaxInputDuration`, then `ShowRecording` only after `StartAsync` completes; release after 1 s → stop, transcribe, insert, `ShowIdle`; release within 200 ms → abort with no transcribe; release while `StartAsync` is still pending → abort once it completes, with no transcribe; cancelled → abort with no insert; a settings save during the recording → `TranscribeAsync` and `InsertAsync` receive the settings from the press; host stop while `StartAsync` is pending → the start's token is cancelled and the controller stops.
- [x] 1.3 Implement processing (trim, empty → `ShowNoSpeech`, insertion outcomes → notification reasons, typed exceptions → their messages, unknown → generic message plus log, `finally` clearing references and `ShowIdle`). Verify: unit tests cover an empty transcript, each non-inserted outcome's notification text (including `ClipboardUnavailable`, which does not claim the text was copied), `LanguageNotSupportedException` → notification, an unknown exception → generic notification with a log entry that does not contain the transcript.
- [x] 1.4 Implement readiness and busy handling (not Ready → reason notification throttled to 10 s per reason, with `FailureMessage` as the reason for `Failed`; press during Processing → `ShowBusy` with no second recording) and max-duration handling (process like release plus notification). Verify: unit tests cover three presses within 5 s while Loading → one notification; a press after 11 s → a second one; a press while `Failed` → the notification carries `FailureMessage`, or the generic failed text when it is `null`; a press during processing → `ShowBusy` with the first dictation still inserted; a press right after `InsertAsync` returned → a new recording starts; `MaxDurationReached` → transcribe, insert and the max-length notification.
- [x] 1.5 Handle recorder failures (start throws → notification and Idle; `Failed` during recording → notification, hide, Idle; `StopAsync` throwing `RecordingFailedException` on release or max duration → notification, hide, Idle; recorder events outside `Recording` dropped; the next press retries). Verify: unit tests with a fake recorder throwing `MicrophoneAccessDeniedException` and `MicrophoneNotRespondingException` on start; raising `Failed` mid-recording; throwing `MicrophoneDisconnectedException` from `StopAsync` on release; raising `Failed` after a release was already handled → no second notification; raising `MaxDurationReached` after a release was already handled → no second `StopAsync`.

## 2. Text insertion

- [x] 2.1 Run the clipboard restore in the background (design D7): `InsertAsync` returns after the paste and keeps the restore as the pending restore; every `InsertAsync` awaits a pending restore first; the restore catches and logs its own failures; `TextInserter` implements `IHostedService`, whose `StopAsync` awaits the pending restore, and `AddTextInsertion` registers one singleton behind `ITextInserter` and `IHostedService`. Adjust the existing restore tests to await the pending restore, including the explicit hardware test `TextInserterHardwareTests.InsertAsync_PasteIntoFocusedTextBox_InsertsTextExactlyAndRestoresClipboard`, which checks the clipboard right after `InsertAsync`. Verify: unit tests with `FakeTimeProvider` cover `InsertAsync` returning `Inserted` before the 750 ms delay has elapsed; a second `InsertAsync` started 300 ms after a paste takes its snapshot only after the first restore, and the clipboard ends with the original content; `StopAsync` completes only after the pending restore; a failing restore does not throw from the next `InsertAsync` or from `StopAsync`; the existing `TextInserterTests` and `TextInsertionServiceCollectionExtensionsTests` pass, and the adjusted hardware test passes when run with `--explicit on`.

## 3. Feedback UI

- [x] 3.1 Add `DictationIcons` (design D5: the four state icons drawn once at startup from the embedded `Tray/TrayIcon.ico` plus a red, amber or grey dot, with ready as the unchanged icon) and implement `DictationFeedback` (design D3: UI-dispatcher marshalling; dictation phase and engine status as the two inputs of one `Render()`; the engine status read at startup and on `StatusChanged`; tooltips "Pisum Transcribe – <state>"; `Notify` via `ShowNotification`). Move tooltip handling out of `TranscriberHostedService`, moving its unit tests along. Verify: a unit test checks that `DictationIcons` creates four distinct 32×32 icons; unit tests with a fake tray service cover the icon and tooltip for each state; every tooltip contains "Pisum Transcribe"; a dictation that ends while the engine is `Loading` shows the unavailable icon with "Loading model…", then ready with "Ready (CPU)" once the engine is `Ready`; an engine that is already `Loading` at startup shows as unavailable; the moved tooltip tests pass.
- [x] 3.2 Add the PerMonitorV2 DPI awareness app manifest and implement `RecordingOverlayWindow` (pill UI, starting look with a grey dot and no text, recording timer, transcribing, busy and no-speech texts; extended styles `WS_EX_NOACTIVATE|WS_EX_TRANSPARENT|WS_EX_TOOLWINDOW|WS_EX_LAYERED` via CsWin32, adding `SetWindowLongPtr`, `GetWindowLongPtr`, `MonitorFromWindow`, `GetMonitorInfo` and `GetDpiForMonitor` to `NativeMethods.txt`; bottom-center placement on the target window's monitor work area). Verify: a unit test of the placement calculation for a given work-area rectangle and DPI. Manually, the overlay appears on the correct monitor, the editor keeps its caret, clicks pass through, and it is absent from Alt+Tab.

## 4. End-to-end verification

- [x] 4.1 Manual MVP run on the target laptop with the default settings (translate de→en, right Ctrl): dictate a German sentence into Notepad, a browser text area, Windows Terminal and Word, and confirm English text is inserted in each. Record the latency from release to text in the change's notes.
- [ ] 4.2 Manual edge cases: tap under 300 ms (nothing happens); right Ctrl+C while holding (cancel, copy still works); switch windows during transcription (clipboard notification); press the hotkey in an elevated terminal (nothing happens, because the hook does not see keys in elevated windows); hold the hotkey in Notepad and click into an elevated terminal (cancelled, nothing inserted); hold right Ctrl and scroll with the mouse (the dictation runs and the overlay is visible); press during transcription ("Still processing…"); copy "invoice 4711", then press the hotkey again right after the first text appears (a new recording starts, and after both dictations the clipboard holds "invoice 4711"); copy "invoice 4711", dictate, and choose **Exit** right after the text appears (the clipboard holds "invoice 4711" after the app has ended); press while the model loads (one notification); microphone privacy off (notification pointing to the microphone privacy settings); microphone muted with the mute key (notification to unmute); a Bluetooth headset as the default recording device (starting look until audio flows, then "Recording", and the first spoken words are transcribed); silence (overlay "No speech detected"); with clipboard history turned on first in Settings › System › Clipboard, because it is off on the target laptop and the checks would otherwise pass trivially, Win+V history does not contain the transcript, and text copied before the dictation appears in it once after the restore; turn clipboard history off again afterwards. Verify: all behave as specified, with results recorded in the change's notes.
- [ ] 4.3 Confirm data retention: after several dictations, search `%LOCALAPPDATA%\Pisum Transcribe\` and the logs for the dictated words, and check that no audio files exist. Verify: no matches.
