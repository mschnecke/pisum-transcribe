## 1. Contract extensions

- [x] 1.1 Add `ISettingsStore.Changed` (`Previous` and `Current`, raised after a successful save). Verify: a unit test confirms the event fires once with correct args after `SaveAsync` and not when the save throws.
- [x] 1.2 Add `IPushToTalkHotkey.SetHotkey`, `Suspend`, `Resume` and the `RawKey` event (raised only while suspended, never logged; `Suspend` and `SetHotkey` reset an active detector with Cancelled). Verify: unit tests with `TestGlobalHook` cover no Pressed while suspended, Cancelled when `Suspend` happens mid-press, RawKey only while suspended, Pressed again after `Resume`, SetHotkey switching the trigger key, and Cancelled when SetHotkey happens mid-press.
- [x] 1.3 Add `IModelStore.Delete` (removes the file; throws for the saved selected model; file errors propagate) and the one-download-per-model check (`InstallAsync` throws `ModelDownloadInProgressException` for a model that is already downloading, without touching the running download). Show "This model is already downloading." in `ModelSetupViewModel`. Verify: unit tests cover deleting an inactive installed model → `IsInstalled` false; deleting the selected model → exception with the file kept; deleting a locked file → exception with `IsInstalled` still true; a second `InstallAsync` of the same model while the first runs → rejected, and the first still installs; the setup view model message for the rejection.
- [x] 1.4 Make `ITranscriber.LoadAsync` valid in every state (design D3: `Loading` at once, requests queued earlier finish on the previous model, dispose before load, newest load wins with no `Ready` in between, `ActiveBackend` null while not `Ready`), remove the `InvalidOperationException` catch from `TranscriberHostedService`, and change `BackendFailedMessage` to point to the backend setting. Verify: unit tests with the fake native seam cover Ready(Vulkan) → reload with CPU → Ready(CPU); a request queued before the reload completes on the previous engine; a request after the reload started is rejected as loading; the old engine is disposed before the new load; a second `LoadAsync` during a running load ends in `Ready` with the second model and backend and raises no `Ready` in between; a queued load superseded by a newer one is never loaded; reload from `Failed`.
- [x] 1.5 Add `ITrayIconService.DoubleClicked`, raised on the UI thread from H.NotifyIcon's `TrayMouseDoubleClick`. Verify: the build passes and the manual check in 4.2 opens the window by double-click.

## 2. Applying settings

- [x] 2.1 Add `IStartupRegistration` and `StartupRegistration` in `SettingsWindow/` (design D7: `IsEnabled` from the Run value and Task Manager's `StartupApproved` value; `SetEnabled(true)` writes the quoted process path and deletes the `StartupApproved` value; `SetEnabled(false)` deletes both; at startup, rewrites the path only when the Run value exists and differs), behind a registry seam. No settings section. Verify: unit tests with a fake registry cover enable, disable, disabled by Task Manager → `IsEnabled` false, enable after Task Manager → override removed, unreadable `StartupApproved` value → enabled, path re-sync when the value differs, and no write at startup when the Run value is missing. Manually, toggle and inspect with `reg query HKCU\Software\Microsoft\Windows\CurrentVersion\Run`.
- [x] 2.2 Implement `SettingsApplier` (design D2 table) and register it in `services.AddSettingsWindow()`, called from `AppHost.Create` after `AddDictation()`. Verify: unit tests with fakes cover a hotkey diff → `SetHotkey` only; a model diff with an installed model → `LoadAsync(new model, backend)`; a model diff with a model that is not installed → no service calls; a backend diff → `LoadAsync(same model, new backend)`; a backend diff while the selected model is not installed → no service calls; a language-only diff → no service calls.

## 3. View models

- [x] 3.1 Extract `ModelDownloadViewModel` from `ModelSetupViewModel` and use it in the setup window. Verify: the existing setup view model tests still pass unchanged.
- [x] 3.2 Implement `LanguageOptions` (source list, target list per task and source, English display names sorted). Verify: unit tests cover de+translate → [English]; en+translate on 1B v2 → 24 non-English languages; the 180M Flash source list → English, French, German, Spanish; transcribe → no target options.
- [x] 3.3 Implement `HotkeyRecorder` (max held set, completes on all released, Esc and `Cancel()` cancel, validation modifier or F1–F24, result as `KeyCode` names) and the `KeyCode` display-name map (sides always named, modifiers first). Verify: unit tests cover left Ctrl+left Win recorded in either order → "Left Ctrl+Left Win"; right Ctrl → "Right Ctrl"; A alone rejected; F13 alone accepted; Esc and `Cancel()` end with no change; the result parses back with `HotkeyParser`.
- [x] 3.4 Implement `SettingsViewModel` and the dictation, text insertion and general section view models (design D1: draft with baseline and per-field dirty tracking, language validation via `TranscriptionOptionsValidator` in source and target error properties, hotkey recording with `Suspend`/`Resume` (design D4), restore-clipboard disabled for Type text, autostart read from `IStartupRegistration`; Save calls `SaveAsync` only for changed settings and `SetEnabled` only for a changed checkbox, then rebases and keeps the window open; `Changed` from elsewhere updates unedited fields; Close discards). Verify: unit tests cover Close leaving the store unchanged; the Polish source + 180M model → error with Save disabled; Save writing the changed fields without a close request and disabling Save afterwards; an external model save while only the hotkey was edited → the saved model is the external one after Save; the restore toggle disabled for `TypeText`; `SetEnabled` called only when the checkbox changed; a failing `SetEnabled` → error shown and the settings still saved.
- [x] 3.5 Implement `ModelSectionViewModel` and `ModelItemViewModel` (design D6: installed-only selection, Delete hidden for the saved and draft-selected model and disabled while `Loading`, delete failure message, "already downloading" message, close-during-download confirmation, installed state refreshed on `ModelInstalled`, delete and activation, engine status line from `ITranscriber`). Verify: unit tests cover Delete unavailable for the saved active model, the draft-selected model and any model while `Loading`; a failed delete → message and still installed; a rejected download → "already downloading"; closing during a download asks and cancels on confirm; the status line following `StatusChanged` through Loading → Ready (CPU).

## 4. Window

- [x] 4.1 Implement the `SettingsDialog` XAML window (left navigation: Dictation, Model, Text insertion, General; bindings; **Save** and **Close** buttons; hotkey **Change…** bound to the dictation section's command, with the recording cancelled on dialog `Deactivated` and `Closed`; Delete confirmation `MessageBox`; engine status line) and `SettingsWindowService` (single instance, activate if open, tray **Settings…** item and `DoubleClicked` handler) in `SettingsWindow/`. Verify manually: the tray item and double-click open one window; a second double-click activates it; all sections render the saved values; Save keeps the window open.
- [ ] 4.2 Manual live-apply check. Verify that each step behaves as specified, and record the results in the change's `notes.md`:
  - Change the hotkey to left Ctrl+left Win and save → it dictates without restart, and right Ctrl no longer triggers.
  - Choose **Change…** and switch to another application → the change is cancelled and push-to-talk works.
  - Switch the backend to CPU → the status line goes Loading → Ready (CPU) while the window stays open.
  - Save backend CPU, then save Auto while the CPU reload is still running → the engine ends Ready on Vulkan, and the log shows the CPU load skipped or released unused. (The startup load takes under 3 s, too short to save during it by hand.)
  - Hold the hotkey, save a different model, and release during the reload → "The model is still loading." notification and nothing inserted.
  - Switch the task to Transcribe (de) → the next dictation inserts German.
  - Switch to Type text → the next dictation is typed.
  - Download 180M Flash in settings, make it active and save → it reloads, and dictation works.
  - Delete the Q8_0 model after switching: Delete is unavailable while loading, and afterwards the file is gone.
  - Start a download in the setup window and the same model in the settings window → "already downloading".
  - Enable Start with Windows → it starts after sign-out and sign-in.
  - Disable it in Task Manager's startup apps → the checkbox reads off; enable and save → it starts after sign-in again.
