## 1. Dependencies, assets and settings

- [x] 1.1 Add `Microsoft.ML.OnnxRuntime` 1.30.0 (exact pin). Add `VoiceActivity/Assets/silero_vad.onnx` from the silero-vad v6.2.2 tag, commit `60b7ffa243625ebdc1070275a29f18c87843786a` (copied to output). Add `THIRD-PARTY-NOTICES.md` with the MIT notices of Silero VAD and ONNX Runtime. Add a bullet to the "Deferred" section of `docs/roadmap.md` about collecting notices for the dependencies that already ship. Verify: `dotnet build` puts `onnxruntime.dll` and `silero_vad.onnx` in the output. Unit tests assert that the model file's SHA-256 is `1a153a22f4509e292a94e67d6f9b85e8deb25b4988682b7e174c65279d8788e3`, and that the loaded runtime version (`OrtEnv.Instance().GetVersionString()`) is `1.30.0`, not the Windows ML copy in System32.
- [x] 1.2 Add `VoiceActivitySettings(Enabled=true)` to `AppSettings` as an `init` section with a `new()` default, and replace a `null` `voiceActivity` section with its default in `JsonSettingsStore.Load`. Verify: settings unit tests check the default `true`, a round trip of `false`, and that a file with `"voiceActivity": null` loads as enabled.

## 2. Detection

- [x] 2.1 Implement `AudioTrimmer.Trim` (null for no segments; first start − 4,800 samples to last end + 4,800 samples, clamped). Verify: unit tests cover no segments → null; a 2 s speech segment in a 6 s clip → about 2.6 s; speech at sample 0 → starts at 0; speech to the end → ends at the last sample; two segments with a pause → pause kept.
- [x] 2.2 Port `SileroVadModel` (context 64 + window 512, state `[2,1,128]`, per-clip reset, pre-allocated `OrtValue` buffers) and `SileroSpeechDetector` (threshold 0.5, negative threshold 0.35, min speech 250 ms, min silence 100 ms). Each ported type names its upstream origin in a `<remarks>` XML doc line, with no file header. Implement `SileroVoiceActivityDetector` (single CPU session created on first use through `Lazy<T>`, 1 thread, lock). Verify: unit tests with the bundled model: 3 s of zeros → no segments; 3 s of white noise at amplitude 0.005 → no segments; state reset between clips gives identical results for the same input run twice.
- [x] 2.3 Implement `VoiceActivityWarmupService` (starts the detector's load and one inference in the background, without awaiting it in `StartAsync`, and logs the loaded ONNX Runtime version), and register the services with `services.AddVoiceActivity()` in `AppHost.Create`, before `AddDictation()`. Verify: unit tests with the bundled model confirm that a `DetectSpeech` call made before the warm-up has finished waits for the load and returns the same segments as a call after it. With a missing model file, the warm-up logs one warning without failing `StartAsync`, and every `DetectSpeech` call throws.
- [x] 2.4 Add explicit hardware tests (skip when `PISUM_TRANSCRIBE_TEST_AUDIO` is missing): the German clip padded with 2 s of silence on each side → at least one segment, and the trimmed padded clip is as long as the trimmed original clip within ±64 ms (2 windows), so the added silence is removed completely whatever silence the clip itself contains; a copy attenuated by −20 dB → still at least one segment; 30 s of audio → `DetectSpeech` median under 300 ms over 5 runs, reported as a diagnostic. Verify: run on the target laptop and record the timing in the change's notes. If the median exceeds 300 ms, apply the two-ended scan fallback from the design's risks before starting 3.1.

## 3. Integration

- [x] 3.1 Integrate VAD into `DictationController` (design D4): take `VoiceActivity.Enabled` at the press into `ActiveDictation`. In `ProcessAsync`, check that flag and trim: null → `ShowNoSpeech` with no transcription; an exception → a warning log and the untrimmed audio. Log only the durations and the VAD time. Verify: unit tests with a fake detector cover disabled → full samples passed; no segments → `TranscribeAsync` not called and `ShowNoSpeech`; segments → trimmed samples passed; detector throws → full samples passed, a warning logged, no notification; VAD enabled at the press and saved as disabled before the release → that dictation is still trimmed.
- [x] 3.2 Add the "Trim silence before transcription" checkbox to the Dictation section of `SettingsDialog.xaml`, bound to a new `DictationSectionViewModel.TrimSilence`. Include it in `Rebase` and in `SettingsViewModel.BuildSettings` (design D4). Verify: `SettingsViewModel` unit tests confirm that toggling makes `HasChanges` true and enables **Save**, that Save writes `VoiceActivity.Enabled`, and that a value saved elsewhere replaces an unedited checkbox but not an edited one. Manually, the checkbox shows the saved value.
- [x] 3.3 Manual end-to-end check: press the hotkey, wait 2 s, speak, wait 2 s and release → the text is inserted, and the log shows the trimmed duration shorter than the original. Hold 3 s silently → "No speech detected" with no transcription logged. Hold 3 s silently next to a running fan → "No speech detected", because the synthetic white noise of the unit tests is only a stand-in for real fan noise. Say only "Ja", with about 1 s of silence before and after → the word is inserted. If it is dropped, lower the minimum speech length as described in the design's risks, and repeat the silent hold. Disable in settings and repeat the silent hold → transcription runs. Verify: record the results in the change's notes.
