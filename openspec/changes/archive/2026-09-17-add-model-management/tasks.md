## 1. Catalog, settings and tray

- [x] 1.1 Add `SpeechModel` and `ModelCatalog` with the three pinned entries from design D1 (URL at revision, size, SHA-256, languages, and the attribution fields `Creator`, `LicenseName`, `LicenseUrl`, `Changes`, `SourceRepository`) and `ModelCatalog.Resolve(id)`, which falls back to `canary-1b-v2-q8_0`. Verify: unit tests confirm three entries, unique IDs, non-empty attribution fields, the default resolution for an unknown ID, and that the 180M Flash languages are exactly de/en/es/fr.
- [x] 1.2 Add `ModelSettings` (`SelectedModelId`, default `canary-1b-v2-q8_0`) to `AppSettings`, and make `JsonSettingsStore` replace a `"model": null` section with the default after loading. Verify: unit tests show that a settings round-trip preserves the value, a missing section yields the default, and `"model": null` yields the default.
- [x] 1.3 Add the `CommunityToolkit.Mvvm` 8.4.2 package reference. Verify: the solution builds.
- [x] 1.4 Add the optional `Func<bool>? isVisible` parameter to `ITrayIconService.AddMenuItem`. `TrayIconService` evaluates each callback on `PreviewTrayContextMenuOpen` and hides the separator above **Exit** when no item above it is visible (design D6). Verify: the solution builds and the existing tests pass. Verify manually: an item whose callback returns false is hidden when the menu opens, and it shows once the callback returns true.

## 2. Model store

- [x] 2.1 Implement `ModelStore.GetModelPath` / `IsInstalled` (file exists and size equals the catalog size) plus startup cleanup of `*.partial` files. Verify: unit tests against a temp models folder cover a missing file, a size mismatch, a correct size, and partial-file cleanup.
- [x] 2.2 Implement `InstallAsync` (design D2): disk-space check (size + 100 MB), `Content-Length` check against the catalog size, streaming download to `.partial` with an incremental SHA-256, abort when more bytes than the catalog size arrive, throttled progress with the catalog size as total, 60 s inactivity timeout, hash compare, atomic move, `ModelInstalled` event, `.partial` deletion on failure or cancel, and the caller's token linked with `ApplicationStopping`. Register `IHttpClientFactory`. Verify: unit tests with a fake `HttpMessageHandler` cover success (event raised, no `.partial`, progress total equals the catalog size), hash mismatch (`ModelIntegrityException`, file deleted), a `Content-Length` that differs from the catalog size (`ModelIntegrityException`, no file written), more bytes than the catalog size (`ModelIntegrityException`, file deleted), mid-stream network exception (file deleted), cancellation (file deleted, stops promptly), application stopping (download cancelled, file deleted) and insufficient disk space (no request sent, through an injectable free-space provider).
- [x] 2.3 Add an explicit (`Explicit = true`, Category `Hardware`) test class `ModelStoreDownloadTests` that downloads `canary-180m-flash-q8_0` for real and asserts it is installed. Verify: `dotnet test Pisum.Transcribe.slnx --filter-class "*.ModelStoreDownloadTests" --explicit on` passes on a machine with internet access.

## 3. Setup window and tray entry

- [x] 3.1 Implement `ModelSetupViewModel` (design D4): model list, selected model, download, cancel and retry commands, progress text "x of y MB" in binary units, error message, and the attribution of the highlighted model. `DownloadCommand` saves `model.selectedModelId` before it calls `InstallAsync`. On success, the view model requests close. Closing during a download asks through a confirmation callback, except after `ApplicationStopping`. Verify: unit tests with a fake `IModelStore` and settings store cover progress text formatting, the selection being saved before `InstallAsync` is called, cancel keeping the new selection, the error-to-retry flow, close on success, and closing during a download (confirm cancels and closes, decline keeps the download running, no confirmation after `ApplicationStopping`, no confirmation without a running download).
- [x] 3.2 Implement `ModelSetupWindow` XAML bound to the view model, with the `Closing` handler and the Yes/No confirmation dialog. Verify manually: the three models show with size and languages; the license panel shows the creator, the license link, the changes and the source repository of the highlighted model; the progress bar moves during a download; closing during a download asks for confirmation, and No keeps the download running.
- [ ] 3.3 Implement `ModelSetupHostedService`: at startup it shows the window without blocking when the selected model is not installed, keeps a single window instance, and adds the tray item **Download model…** with an `isVisible` callback that returns true while the selected model is not installed (design D6). Verify manually: on a clean `%LOCALAPPDATA%\Pisum Transcribe\`, the window opens at start. Closing it leaves the tray item, which reopens the window. After a successful download, the item is gone the next time the menu opens. Deleting the model file while the app runs brings the item back. Choosing **Exit** during a download ends the app within 5 seconds, and no `.partial` file remains after the next start.
