## 1. Solution and build setup

- [x] 1.1 Add `Directory.Build.props` (Nullable, ImplicitUsings, LangVersion latest, TreatWarningsAsErrors, GenerateDocumentationFile) and `Directory.Packages.props` with central package management. Verify: `dotnet build Pisum.Transcribe.slnx` succeeds once 1.2 exists.
- [x] 1.2 Create `src/Pisum.Transcribe/Pisum.Transcribe.csproj` (WinExe, `net10.0-windows`, UseWPF, `win-x64`, no SupportedOSPlatformVersion (a value above 7.0 needs a versioned TFM, NETSDK1135), Product "Pisum Transcribe", InternalsVisibleTo `Pisum.Transcribe.Tests`, StartupObject `Pisum.Transcribe.Program`) with `Program.cs` (`[STAThread]` `Main` that creates `App`, calls `InitializeComponent()` and `Run()`, and returns the exit code), `App.xaml` (ShutdownMode OnExplicitShutdown, no StartupUri), `GlobalUsing.cs` and an embedded placeholder tray `.ico` (final status icons come with `add-dictation-workflow`). Register it with `dotnet sln Pisum.Transcribe.slnx add`. Verify: `dotnet build Pisum.Transcribe.slnx` succeeds.
- [x] 1.3 Create `tests/Pisum.Transcribe.Tests/Pisum.Transcribe.Tests.csproj` (Exe, `net10.0-windows`, xunit.v3 4.0.1, Shouldly 4.3.0, FakeItEasy 9.0.1, project reference to the app) with `Traits.cs` and a placeholder unit test. Register it in the solution. Verify: `dotnet test Pisum.Transcribe.slnx` discovers and passes the test.

## 2. Hosting, paths and logging

- [x] 2.1 Implement `AppPaths` (root `%LOCALAPPDATA%\Pisum Transcribe\`, `logs\`, `models\`, creates the root on demand, root overridable for tests). Verify: unit tests confirm the paths resolve under an injected root and the root folder is created.
- [x] 2.2 Build the Generic Host with `Host.CreateEmptyApplicationBuilder` in `App.OnStartup` and start it. Set `HostOptions.ShutdownTimeout` to 4 s and `BackgroundServiceExceptionBehavior` to `StopHost`. Add a `services.Add<Feature>()` extension per feature folder. Stopping the host belongs to `ShutdownCoordinator` (3.3). Verify: the app launches with no errors in the log, and no `appsettings.json`, console or Event Log provider is registered.
- [x] 2.3 Configure Serilog: a bootstrap logger (`CreateBootstrapLogger()`) created in `Main`, replaced by `services.AddSerilog(...)` once the host is built; daily rolling file under `logs\`, 7 files retained; startup entry with the app version; `Log.CloseAndFlush()` in `Main`'s `finally`. Hook `AppDomain.UnhandledException` (log fatal, then flush) and `TaskScheduler.UnobservedTaskException` (log error, keep running). `Dispatcher.UnhandledException` is handled in 3.3. Verify: after launching, today's log file contains the version entry. A unit test confirms the unobserved-task-exception handler logs the exception through a fake logger.

## 3. Tray shell and single instance

- [x] 3.1 Implement `SingleInstanceGuard` (design D4): named mutex `Local\Pisum.Transcribe.SingleInstance` passed through the constructor, `WaitOne` for up to 6 s, `AbandonedMutexException` counts as acquired, released on the owning thread. Call it first in `Main`, before the bootstrap logger. On timeout, exit with code 0. Verify: unit tests with a unique mutex name per test, where the first owner is on a second thread (a mutex is re-entrant on its own thread), confirm that acquisition times out while the other thread owns it, succeeds after release, and succeeds with the abandoned case when the owning thread ends without releasing. Manually: a second launch exits and leaves one tray icon, and a launch right after **Exit** starts once the old process has ended.
- [x] 3.2 Implement `TrayIconService` (H.NotifyIcon.Wpf `TaskbarIcon` created in code and shown with `ForceCreate(enablesEfficiencyMode: false)`, tooltip "Pisum Transcribe", context menu with **Exit** raising `ExitRequested`, plus `AddMenuItem`, `SetStatus` and `ShowNotification` for later changes). Verify manually: the icon appears with no window or taskbar button, and Task Manager does not show "Efficiency mode" for the process.
- [x] 3.3 Implement `ShutdownCoordinator` (design D2): `RequestShutdown(UserExit | Error)` runs once; triggers are `TrayIconService.ExitRequested`, `Dispatcher.UnhandledException` (log, `Handled = true`, `Error`) and `ApplicationStopping` without a requested shutdown (`Error`); sequence is watchdog start (4.5 s, then `TerminateProcess`; see D2), icon removal (at once for `UserExit`, last for `Error`, at least 3 s after the error notification), awaited `host.StopAsync()`, host disposal, `Application.Current.Shutdown(exit code)`; exit code 0 for `UserExit` and 1 for `Error`. Inject `TimeProvider` and the process-exit action so the watchdog is testable. Verify: unit tests with fakes confirm a second `RequestShutdown` is ignored, `ApplicationStopping` without a request leads to `Error` with exit code 1, the icon stays 3 s after the error notification, and a hosted service that never stops triggers the exit action at 4.5 s. Manually: **Exit** removes the icon and ends the process within 5 s; a test exception thrown on the UI thread shows the error notification, ends the process within 5 s with exit code 1, and the log contains the exception.

## 4. Settings storage

- [x] 4.1 Implement `AppSettings` (record with `SchemaVersion = 1`, with the shape rules from design D6 in its XML doc), `ISettingsStore` and `JsonSettingsStore` (camelCase properties, camelCase string enums, unknown properties ignored, load at startup). Verify: unit tests cover the defaults when the file is missing, an empty object `{}` and a file containing only `null` loading as defaults, a partial file keeping its values, and unknown properties being ignored.
- [x] 4.2 Add corrupt-file recovery (rename to `settings.json.corrupt`, overwrite an older one, warn, use defaults). Verify: a unit test with invalid JSON checks the rename, the defaults and the logged warning (fake logger).
- [x] 4.3 Add safe writes (write `settings.json.tmp`, flush, `File.Move(..., overwrite: true)`). Verify: a unit test confirms a stale `settings.json.tmp` does not affect loading and a save leaves no `.tmp` behind.

## 5. Documentation

- [x] 5.1 Update `CLAUDE.md`: replace the "fresh scaffold" section with the project layout, feature folders, conventions (no transcript or audio in logs, data under `%LOCALAPPDATA%\Pisum Transcribe\`, explicit hardware tests) and test commands, and correct the SDK version to `10.0.400`. Verify: the commands in `CLAUDE.md` run successfully as written.
