## 1. Packages and settings

- [x] 1.1 Add `SharpHook` 8.0.0 and `NAudio.Wasapi` 3.1.0 (exact pins) to the app, and `SharpHook.Testing` 8.0.0 to the test project. Verify: `dotnet build` succeeds and `uiohook.dll` is in the build output.
- [x] 1.2 Add `RecordingSettings` (D4: `Hotkey` as an `init` property with default `["VcRightControl"]`), the `Recording` section in `AppSettings` with its `null` fallback in `JsonSettingsStore.Load`, and parsing into a `KeyCode` set (case-insensitive; `null`, empty or unknown → default plus a warning). Verify: unit tests cover the default, a valid two-key hotkey, a camelCase key name, an unknown name falling back with a logged warning, an empty list falling back, and a settings file with `"recording": null` loading the default section.

## 2. Push-to-talk hotkey

- [x] 2.1 Implement `PushToTalkDetector` (D1), including `DownKeys`. Verify: unit tests for each `push-to-talk-hotkey` scenario: single key press and release, multi-key any order, auto-repeat ignored, other key → Cancelled with no Released afterwards, next press works after cancel, Reset while active → Cancelled, Reset clearing the suppressed state after a cancel, and `DownKeys` holding only hotkey keys.
- [x] 2.2 Implement `SharpHookPushToTalkHotkey` (D2: `EventLoopGlobalHook` with a background event loop, started with `RunAsync(GlobalHookType.Keyboard, useBackgroundThread: true)`; simulated events dropped; raw key codes recorded; detector calls and signal raising under one lock; `Pressed`/`Released`/`Cancelled` events; hook failure → error log plus tray notification; hook disposed on stop). Add `services.AddRecording()`, registering it as `IPushToTalkHotkey` and hosted service plus `TimeProvider.System` with `TryAddSingleton`, and call it from `AppHost.Create`. Log signals at Debug level with no key text. Verify: unit tests with `SharpHook.Testing.TestGlobalHook` simulate key events and assert the raised events, assert that events marked simulated through `EventMask` raise, release and cancel nothing, assert that a failing `RunResult` logs an error and shows the notification, and, with `CapturingLogger`, assert that after simulated typing of several letters and a right Ctrl+C cancel no log entry contains those keys' names or raw codes. Manually, holding right Ctrl in Notepad logs Pressed and Released, and right Ctrl+C logs Cancelled while Notepad still copies. Then keep the app running with the hook on the target laptop for about an hour of normal work and check the endpoint protection for detections or quarantined files; if it reacts, stop and involve IT before continuing. Record the outcome in the change's notes.
- [x] 2.3 Implement the missed-release check (D3: `TimeProvider` timer every 250 ms while `DownKeys` is not empty; `GetAsyncKeyState` through an injected `Func<int, bool>` with the recorded raw key codes; two consecutive up readings → `Reset()`). Verify: unit tests with `FakeTimeProvider` and a fake key state cover a held key reading up on two ticks → Cancelled, and the next press raising Pressed; an up reading followed by a down reading → no cancel; a key-up event arriving between ticks → Released and no Cancelled; the timer stopping when no keys are down; leftover state after a cancel cleared without another Cancelled. Manually: (a) hold right Ctrl in Notepad and click into an elevated Windows Terminal → Cancelled logged within 1 s, and the next press in Notepad logs Pressed; (b) run `Start-Sleep 3; Start-Process notepad -Verb RunAs`, hold right Ctrl until the UAC prompt appears, release it there → Cancelled within 1 s; (c) run `Start-Sleep 3; rundll32 user32.dll,LockWorkStation`, hold right Ctrl until the screen locks, release, unlock → Cancelled logged, and the next press logs Pressed. Record the outcomes in the change's notes.

## 3. Audio recording

- [x] 3.1 Spike: how Windows reports blocked access and mute (D5 open point). On the target laptop, with a temporary explicit test that builds a `WasapiRecorderBuilder` with default-device routing and the 16 kHz mono float format and records 2 s, turn off each switch under Settings › Privacy & security › Microphone in turn (*Microphone access*, *Let apps access your microphone*, *Let desktop apps access your microphone*) and record whether the start fails, with which exception and HRESULT, or delivers only silence, and whether those packets carry the WASAPI silent flag. Then press the laptop's microphone mute key and confirm that `AudioEndpointVolume.Mute` of the default capture endpoint reads `true`. Verify: the outcomes are recorded in the change's notes; if any switch delivers silence, add the `ConsentStore` check from D5 to task 3.5 before starting it.
- [x] 3.2 Add `IAudioRecorder`, `AudioClip`, the abstract `RecordingFailedException` and its subclasses (`MicrophoneAccessDeniedException`, `NoMicrophoneException`, `MicrophoneMutedException`, `MicrophoneNotRespondingException`, `MicrophoneDisconnectedException`). Verify: builds, and unit tests assert that the access-denied message points to "Settings › Privacy & security › Microphone" and mentions desktop apps, and that the muted message tells the user to unmute the microphone.
- [x] 3.3 Implement a `SampleAccumulator` (append float spans, clamping each sample to [-1, 1], enforce the max sample count and report that the limit was hit, snapshot, clear). Verify: unit tests cover appending across buffers, values outside [-1, 1] clamped to the bounds, truncation exactly at `maxDuration × 16000` with the limit flag, and clear-on-abort.
- [x] 3.4 Add `ICaptureSessionFactory` and `ICaptureSession`, and implement `AudioRecorder` against them with the D5 contract (states `Idle | Recording | LimitReached | Failed`, start completing on the first packet that is neither flagged silent nor all zeros, with leading packets of either kind dropped, 3 s start timeout through the injected `TimeProvider`, stored error, limit stop queued off the capture callback, stop winning over a concurrent session error, pending session stops awaited, `DisposeAsync` on all paths). Register it in `AddRecording()`. Verify: unit tests with a fake session and `FakeTimeProvider` cover every cell of the contract table; `StartAsync` completing only on the first real packet, with leading flagged-silent and all-zero packets absent from the clip and later ones kept; no real packet within 3 s → `MicrophoneNotRespondingException` with the session disposed; cancelling a pending start; `StopAsync` or `AbortAsync` during a pending start throwing `InvalidOperationException`; a start failure → `Idle` with the typed exception; limit reached → session stopped, `MaxDurationReached` raised once and `StopAsync` returning the kept audio; a session error → `Failed` raised once, `StopAsync` throwing `MicrophoneDisconnectedException` once, and a following `StartAsync` succeeding; `AbortAsync` never throwing; a stop racing a session error returning the audio without `Failed`; `DisposeAsync` while recording and while a start is pending disposing the session, with no events afterwards and later calls throwing `ObjectDisposedException`.
- [x] 3.5 Implement `WasapiCaptureSession` and its factory (D5: mute check on the default capture endpoint before building; per-recording `WasapiRecorderBuilder` with default-device routing, 16 kHz mono float format and 20 ms buffers, built off the UI thread; span reinterpreted as floats and passed with the WASAPI silent flag, a flagged packet as zeros; HRESULT → exception mapping as a pure function). Verify: unit tests for the mapping function, then the hardware tests in 3.6.
- [x] 3.6 Add explicit hardware tests (Category `Hardware`): record 2 s → about 32,000 samples all within [-1, 1]; measure the `StartAsync` duration, which ends at the first real audio, and report it (flag if > 200 ms); run 100 warm-up start/stop cycles, then 50 more, and check the process handle count stays within +20 over those 50; abort returns nothing. If the 16 kHz mono float format is rejected, stop and revisit D5's format conversion. Verify: run with explicit tests enabled on the target laptop and record the latency in the change's notes.
- [x] 3.7 Manual error checks: (a) turn off each of the three switches under Settings › Privacy & security › Microphone in turn and confirm the start fails with `MicrophoneAccessDeniedException`; (b) disable all input devices and confirm `NoMicrophoneException`; (c) switch the default device mid-recording and confirm the recording continues; (d) with only a USB microphone enabled, unplug it mid-recording and record whether `Failed` is raised with `MicrophoneDisconnectedException` or capture stalls without samples (D5 open point); (e) press the microphone mute key and confirm the start fails with `MicrophoneMutedException` and Windows shows no microphone-in-use indicator; (f) with a Bluetooth headset as the default recording device, record the `StartAsync` duration. Use the explicit test harness from 3.6. Verify: record the outcomes in the change's notes.
